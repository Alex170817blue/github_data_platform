name: github-data-platform

deployments:
  - name: run-full-pipeline
    entrypoint: flows/run_pipeline.py:run_pipeline_flow
    work_pool:
      name: default-pool
      job_variables:
        working_dir: /app